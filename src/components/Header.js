import React from "react";

const Header = () => {
  // static header in all pages
  return (
    <div>
      <h1 style={{ textAlign: "center", color: "gold", fontWeight: "bold" }}>
        Welcome to Maths Quiz
      </h1>
    </div>
  );
};

export default Header;
